#include <iostream>

using namespace std;
int main()
{
    // int x,y;
    // cin >> x >> y;
    // cout<<"value of x : "<<x <<" and value of y is : "<<y;
    // return 0;

    // getline for stringd
    //  string str;
    //  getline(cin,str);
    //  cout<<str;
    //  return 0;

    // wap to check you are adult or not
    int age;
    cin >> age;
    if (age >= 18)
    {
        cout << "you are adult";
    }
    else
    {
        cout << "you are not an adult";
    }

    return 0;
}